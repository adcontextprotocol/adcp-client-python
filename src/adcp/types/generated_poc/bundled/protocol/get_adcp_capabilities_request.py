# generated by datamodel-codegen:
#   filename:  bundled/protocol/get_adcp_capabilities_request.json
#   timestamp: 2026-04-29T01:11:23+00:00

from __future__ import annotations

from enum import Enum
from typing import Annotated, Any

from adcp.types.base import AdCPBaseModel
from pydantic import ConfigDict, Field


class Protocol(Enum):
    media_buy = "media_buy"
    signals = "signals"
    governance = "governance"
    sponsored_intelligence = "sponsored_intelligence"
    creative = "creative"


class GetAdcpCapabilitiesRequest(AdCPBaseModel):
    model_config = ConfigDict(
        extra="allow",
    )
    adcp_major_version: Annotated[
        int | None,
        Field(
            description="The AdCP major version the buyer's payloads conform to. When provided, the seller validates this against its supported major_versions and returns VERSION_UNSUPPORTED if the version is not in range. When omitted, the seller assumes the highest major version it supports.",
            ge=1,
            le=99,
        ),
    ] = None
    protocols: Annotated[
        list[Protocol] | None,
        Field(
            description="Specific protocols to query capabilities for. If omitted, returns capabilities for all supported protocols.",
            min_length=1,
        ),
    ] = None
    context: Annotated[
        dict[str, Any] | None,
        Field(
            description="Opaque correlation data that is echoed unchanged in responses. Used for internal tracking, UI session IDs, trace IDs, and other caller-specific identifiers that don't affect protocol behavior. Context data is never parsed by AdCP agents - it's simply preserved and returned.",
            title="Context Object",
        ),
    ] = None
    ext: Annotated[
        dict[str, Any] | None,
        Field(
            description="Extension object for platform-specific, vendor-namespaced parameters. Extensions are always optional and must be namespaced under a vendor/platform key (e.g., ext.gam, ext.roku). Used for custom capabilities, partner-specific configuration, and features being proposed for standardization.",
            title="Extension Object",
        ),
    ] = None
