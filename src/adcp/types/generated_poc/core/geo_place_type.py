# generated by datamodel-codegen:
#   filename:  core/geo_place_type.json
#   timestamp: 2026-08-17T23:02:13+00:00

from __future__ import annotations

from adcp.types._str_enum import StrEnum
from typing import Any, Annotated

from pydantic import AnyUrl, Field, RootModel


class GeographicPlaceType1(StrEnum):
    airport = 'airport'
    borough = 'borough'
    city = 'city'
    city_region = 'city_region'
    commune = 'commune'
    county = 'county'
    district = 'district'
    municipality = 'municipality'
    neighborhood = 'neighborhood'
    post_town = 'post_town'
    prefecture = 'prefecture'
    province = 'province'
    quarter = 'quarter'
    state = 'state'
    territory = 'territory'
    ward = 'ward'


class GeographicPlaceType2(RootModel[AnyUrl]):
    root: Annotated[
        AnyUrl,
        Field(
            description='Canonical place classification. Registered tokens have protocol-defined meanings. Catalog-specific classifications without a registered mapping MUST use an absolute HTTPS URI controlled by the vocabulary owner; consumers compare URI types as exact opaque strings.',
            examples=[
                'city',
                'municipality',
                'borough',
                'neighborhood',
                'post_town',
                'city_region',
                'county',
                'https://seller.example/geo/place-types/trade-area',
            ],
            title='Geographic Place Type',
        ),
    ]


class GeographicPlaceType(RootModel[GeographicPlaceType1 | GeographicPlaceType2]):
    root: Annotated[
        GeographicPlaceType1 | GeographicPlaceType2,
        Field(
            description='Canonical place classification. Registered tokens have protocol-defined meanings. Catalog-specific classifications without a registered mapping MUST use an absolute HTTPS URI controlled by the vocabulary owner; consumers compare URI types as exact opaque strings.',
            examples=[
                'city',
                'municipality',
                'borough',
                'neighborhood',
                'post_town',
                'city_region',
                'county',
                'https://seller.example/geo/place-types/trade-area',
            ],
            title='Geographic Place Type',
        ),
    ]
    def __getattr__(self, name: str) -> Any:
        """Proxy attribute access to the wrapped type."""
        if name.startswith('_'):
            raise AttributeError(name)
        return getattr(self.root, name)
